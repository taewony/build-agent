system: ChatBotSystem
component: ChatBot
tests:
  - name: test_basic_chat
    function: chat
    input: {user_message: "Hello!"}
    # We can't predict the exact LLM response, so we just check if it's a string
    # (The verifier might need to be enhanced for this, but for now we'll mock it or use PASSED)
    expected: "Hello! How can I help you today?" 
